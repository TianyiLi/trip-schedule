import React from 'react';
import TripPlanningContainer from '../container/TripPlanningContainer';

const TripPlanningPage: React.FC = () => {
  return <TripPlanningContainer />;
};

export default TripPlanningPage;